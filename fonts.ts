import { Inter, Abel } from "next/font/google";

export const inter = Inter({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const able = Abel({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
